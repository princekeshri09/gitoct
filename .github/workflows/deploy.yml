name: hello-world
on:
  workflow_dispatch:
    inputs:
      name:
        type: choice
        description: Select the release
        options:
          - monalisa
          - cschleiden
      message:
        required: true
      environment:
        type: environment
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"
